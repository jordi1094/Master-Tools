import {z} from 'zod'

const CharacterSchema = z.object({
    acrobatics: z.number().min(0).max(30),
    alignment: z.string(),
    image: z.string().url(),
    animalHandling: z.number().min(0).max(30),
    arcana: z.number().min(0).max(30),
    armorClass: z.number().min(0).max(30),
    athletics: z.number().min(0).max(30),
    attacksAndSpellcasting: z.string(),
    background: z.string(),
    bonds: z.string(),
    charismaDeathSave: z.number().min(0).max(20),
    charismaModifier: z.number().min(-5).max(10),
    charismaScore: z.number().min(1).max(30),
    classCharacter: z.string(),
    constitutionDeathSave: z.number().min(0).max(20),
    constitutionModifier: z.number().min(-5).max(10),
    constitutionScore: z.number().min(1).max(30),
    cooper: z.number().min(0).max(999999),
    currentHitPoints: z.number().min(0).max(999),
    deathSavesFailures: z.number().min(0).max(3),
    deathSavesSucceses: z.number().min(0).max(3),
    deception: z.number().min(0).max(30),
    dexterityDeathSave: z.number().min(0).max(20),
    dexterityModifier: z.number().min(-5).max(10),
    dexterityScore: z.number().min(1).max(30),
    electrum: z.number().min(0).max(999999),
    equipment: z.string(),
    exp: z.number().min(0).max(355000),
    featuresAndTraits: z.string(),
    flaws: z.string(),
    gold: z.number().min(0).max(999999),
    history: z.number().min(0).max(30),
    hitDice: z.string(),
    ideals: z.string(),
    iniciative: z.number().min(0).max(30),
    iniciativeDeathSave: z.number().min(0).max(20),
    initiativeModifier: z.number().min(-5).max(10),
    initiativeScore: z.number().min(1).max(30),
    insight: z.number().min(0).max(30),
    inspiration: z.boolean(),
    intimidation: z.number().min(0).max(30),
    investigation: z.number().min(0).max(30),
    level: z.number().min(1).max(20),
    maxHitPoints: z.number().min(1).max(999),
    medicine: z.number().min(0).max(30),
    name: z.string(),
    nature: z.number().min(0).max(30),
    otherProeficiencesAndLanguages: z.string(),
    perception: z.number().min(0).max(30),
    performance: z.number().min(0).max(30),
    personalityTraits: z.string(),
    persuasion: z.number().min(0).max(30),
    platinium: z.number().min(0).max(999999),
    proficiencyBonus: z.number().min(2).max(6),
    race: z.string(),
    religion: z.number().min(0).max(30),
    silver: z.number().min(0).max(999999),
    sleightOfHand: z.number().min(0).max(30),
    speed: z.number().min(0).max(120),
    strengthDeathSave: z.number().min(0).max(20),
    strengthModifier: z.number().min(-5).max(10),
    strengthScore: z.number().min(1).max(30),
    survival: z.number().min(0).max(30),
    temporalHitPoints: z.number().min(0).max(999),
    wisdomModifier: z.number().min(-5).max(10),
    wisdomScore: z.number().min(1).max(30),
    wishdomDeathSave: z.number().min(0).max(20),
});

export default CharacterSchema